import { PageTransition } from '../components/layout/PageTransition';

export function TrendsPage() {
  return (
    <PageTransition>
      <h1>Trends</h1>
      <p style={{ color: 'var(--color-text-secondary)' }}>Trends dashboard</p>
    </PageTransition>
  );
}
